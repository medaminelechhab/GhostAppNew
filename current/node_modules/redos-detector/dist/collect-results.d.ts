import { Trail } from './checker-reader';
import { MyRootNode } from './parse';
import { RedosDetectorError } from './redos-detector';
export type WalkerResult = Readonly<{
    error: RedosDetectorError | null;
    trails: readonly Trail[];
    worstCaseBacktrackCount: number;
}>;
export type CollectResultsInput = Readonly<{
    atomicGroupOffsets: ReadonlySet<number>;
    caseInsensitive: boolean;
    dotAll: boolean;
    maxBacktracks: number;
    maxSteps: number;
    multiLine: boolean;
    node: MyRootNode;
    timeout: number;
}>;
export declare function collectResults({ atomicGroupOffsets, node, maxBacktracks, maxSteps, multiLine, timeout, caseInsensitive, dotAll, }: CollectResultsInput): WalkerResult;
